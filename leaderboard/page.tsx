"use client";
import { useEffect,useState } from "react";
export default function Leaderboard(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{fetch('/api/leaderboard').then(r=>r.json()).then(setRows)},[]);return <div className="container"><h1 className="title">Leaderboard</h1><p className="subtitle">أفضل الملتزمين حسب XP والنقاط.</p><div className="card pad" style={{marginTop:16}}><div className="list">{rows.map((r)=><div className="row" key={r.id}><div style={{display:'flex',alignItems:'center',gap:10}}><strong style={{width:28}}>#{r.rank}</strong>{r.image?<img className="avatar" src={r.image} alt=""/>:<div className="avatar"/>}<div><strong>{r.name||'مستخدم Commit'}</strong><div className="tiny muted">Level {r.level}</div></div></div><div style={{textAlign:'left'}}><strong>{r.xp} XP</strong><div className="tiny muted">{r.points} نقطة</div></div></div>))}</div>{rows.length===0&&<div className="empty">لا توجد بيانات بعد.</div>}</div></div>}
