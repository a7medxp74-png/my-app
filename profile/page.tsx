"use client";
import { useEffect,useState } from "react";
export default function Profile(){const [m,setM]=useState<any>();useEffect(()=>{fetch('/api/me').then(r=>r.json()).then(setM)},[]);if(!m)return <div className="container"><div className="card pad">جاري التحميل…</div></div>;return <div className="container"><div className="card pad" style={{textAlign:'center'}}>{m.user.image?<img className="avatar" style={{width:78,height:78}} src={m.user.image} alt=""/>:<div className="avatar" style={{width:78,height:78,margin:'auto'}}/>}<h1 className="title" style={{marginTop:12}}>{m.user.name||'مستخدم Commit'}</h1><p className="muted">{m.user.email}</p></div><div className="grid four" style={{marginTop:14}}><div className="card stat"><div className="stat-value">{m.level.level}</div><div className="stat-label">Level</div></div><div className="card stat"><div className="stat-value">{m.user.xp}</div><div className="stat-label">XP</div></div><div className="card stat"><div className="stat-value">{m.user.points}</div><div className="stat-label">النقاط</div></div><div className="card stat"><div className="stat-value">{m.stats.successful}</div><div className="stat-label">جلسات ناجحة</div></div></div></div>}
