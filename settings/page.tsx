"use client";
import { useEffect,useState } from "react";
export default function Settings(){const [dark,setDark]=useState(false);useEffect(()=>{const d=localStorage.getItem('commit-theme')==='dark';setDark(d);document.documentElement.dataset.theme=d?'dark':''},[]);function toggle(){const d=!dark;setDark(d);document.documentElement.dataset.theme=d?'dark':'';localStorage.setItem('commit-theme',d?'dark':'light')}return <div className="container"><h1 className="title">الإعدادات</h1><p className="subtitle">تخصيص تجربة Commit على جهازك.</p><div className="card pad" style={{marginTop:16}}><div className="row"><div><strong>الوضع الداكن</strong><div className="tiny muted">مريح للعين في جلسات المساء.</div></div><button onClick={toggle} className={`btn ${dark?'btn-primary':'btn-secondary'}`}>{dark?'مفعّل':'غير مفعّل'}</button></div></div><div className="card pad" style={{marginTop:12}}><strong>الأمان</strong><p className="tiny muted">بيانات الجلسات والنقاط والتحقق تُدار من السيرفر. لا تستخدم Commit localStorage كقاعدة بيانات.</p></div></div>}
