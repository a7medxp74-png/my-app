"use client";
import { useEffect,useState } from "react";
export default function OwnerUsers(){const [data,setData]=useState<any>({items:[]});const [q,setQ]=useState('');async function load(){const r=await fetch('/api/owner/users?q='+encodeURIComponent(q));setData(await r.json())}useEffect(()=>{load()},[]);async function toggle(id:string,status:string){await fetch(`/api/owner/users/${id}/status`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({status:status==='ACTIVE'?'DISABLED':'ACTIVE'})});load()}return <div className="container"><h1 className="title">إدارة المستخدمين</h1><div className="card pad" style={{marginTop:16}}><input className="input" placeholder="بحث بالاسم أو البريد" value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&load()}/><div className="table-wrap"><table className="table"><thead><tr><th>المستخدم</th><th>XP</th><th>Points</th><th>جلسات</th><th>الحالة</th><th/></tr></thead><tbody>{data.items.map((u:any)=><tr key={u.id}><td>{u.name||u.email}</td><td>{u.xp}</td><td>{u.points}</td><td>{u._count.studySessions}</td><td>{u.status}</td><td><button className="btn btn-secondary" onClick={()=>toggle(u.id,u.status)}>{u.status==='ACTIVE'?'تعطيل':'تفعيل'}</button></td></tr>)}</tbody></table></div></div></div>}
